const $=(s,c=document)=>c.querySelector(s);const $$=(s,c=document)=>[...c.querySelectorAll(s)];const esc=s=>String(s??'').replace(/[&<>'"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[m]));const local=['localhost','127.0.0.1'].includes(location.hostname)||location.protocol==='file:';let profile;
async function getProfile(){if(window.__PROFILE_DATA__)return window.__PROFILE_DATA__;if(local){const s=localStorage.getItem('sk_profile_preview_v7');if(s)try{return JSON.parse(s)}catch{}}try{const r=await fetch('/api/profile',{cache:'no-store'});if(r.ok)return r.json()}catch{}return fetch('assets/profile.json',{cache:'no-store'}).then(r=>r.json())}
function initTheme(){const saved=localStorage.getItem('sk_theme_v7');const theme=saved||((matchMedia('(prefers-color-scheme: light)').matches)?'light':'dark');document.documentElement.dataset.theme=theme;$('#themeBtn').onclick=()=>{const n=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;localStorage.setItem('sk_theme_v7',n)}}
function initMenu(){const b=$('#menuBtn'),n=$('#mobileNav');b.onclick=()=>{const open=n.classList.toggle('open');b.setAttribute('aria-expanded',open)};$$('a',n).forEach(a=>a.onclick=()=>{n.classList.remove('open');b.setAttribute('aria-expanded','false')})}
function renderFields(p){$$('[data-field]').forEach(el=>{const v=p[el.dataset.field]??'';if(el.tagName==='INPUT'||el.tagName==='TEXTAREA')el.value=v;else el.textContent=v})}
function renderStats(p){$('#stats').innerHTML=(p.stats||[]).map(s=>`<article class="stat-card"><strong>${esc(s.value)}</strong><span>${esc(s.label)}</span></article>`).join('')}
const initials=t=>String(t).split(/\s|\//).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase();
function renderOrbit(p){const mount=$('#portraitOrbit');if(!mount)return;const labels=p.orbitLabels||['Automation','Observability','Cloud','CI/CD','AI','DevOps'];mount.innerHTML=labels.map((label,i)=>`<span class="orbit-label" style="--i:${i};--count:${labels.length}"><span>${esc(label)}</span></span>`).join('')}
function renderRibbon(p){const tools=[...new Set((p.toolGroups||[]).flatMap(g=>g.tools||[]))];const html=tools.map(t=>`<span class="tool-chip"><i>${esc(initials(t))}</i>${esc(t)}</span>`).join('');$('#toolRibbon').innerHTML=html+html}
function renderPlatforms(p){const list=(p.toolGroups?.[0]?.tools||['Windows','Linux','Unix','Cloud','H/W Networking']);$('#platformList').innerHTML=list.map(x=>`<li>${esc(x)}</li>`).join('')}
function renderTools(p){$('#toolsGrid').innerHTML=(p.toolGroups||[]).map((g,i)=>`<article class="tool-group" style="--accent:${['rgba(42,168,255,.24)','rgba(49,216,196,.24)','rgba(156,99,255,.24)','rgba(255,189,89,.2)','rgba(66,226,165,.2)'][i%5]}"><div class="tool-icon">${esc(initials(g.title))}</div><h3>${esc(g.title)}</h3><p>${esc(g.outcome||'')}</p><div class="tool-pills">${(g.tools||[]).map(t=>`<span>${esc(t)}</span>`).join('')}</div></article>`).join('')}
function renderProjects(p){$('#projectList').innerHTML=(p.projects||[]).map(x=>`<article class="project-card tilt-card"><span class="type">${esc(x.type)}</span><h3>${esc(x.name)}</h3><p>${esc(x.summary)}</p><div class="project-tags">${(x.tags||[]).map(t=>`<span>${esc(t)}</span>`).join('')}</div><div class="project-links"><a href="${esc(x.url||x.github)}" ${String(x.url||'').startsWith('http')?'target="_blank" rel="noopener"':''}>View project</a><a href="${esc(x.github)}" target="_blank" rel="noopener">GitHub</a></div><div class="project-visual" aria-hidden="true"></div></article>`).join('')}
function renderExperience(p){const tabs=$('#expTabs'),panel=$('#expPanel');tabs.innerHTML=(p.experience||[]).map((x,i)=>`<button class="${i===0?'active':''}" type="button" data-index="${i}" role="tab"><strong>${esc(x.title)}</strong><span>${esc(x.period)}</span></button>`).join('');const show=i=>{const x=p.experience[i];panel.innerHTML=`<span class="period">${esc(x.period)}</span><h3>${esc(x.title)}</h3><h4>${esc(x.company)}${x.location?' · '+esc(x.location):''}</h4><p>${esc(x.details)}</p><div class="highlight-list">${(x.highlights||[]).map(h=>`<span>${esc(h)}</span>`).join('')}</div>`;$$('button',tabs).forEach((b,j)=>b.classList.toggle('active',j===i))};tabs.onclick=e=>{const b=e.target.closest('[data-index]');if(b)show(+b.dataset.index)};show(0);$('#educationPanel').innerHTML=`<h3>Education & professional training</h3>${(p.education||[]).map(x=>`<article class="education-entry"><small>${esc(x.period)}</small><strong>${esc(x.qualification)}</strong><span>${esc(x.institution)}</span>${x.details?`<p>${esc(x.details)}</p>`:''}</article>`).join('')}`}
function renderCerts(p){$('#certGrid').innerHTML=(p.certifications||[]).map(x=>`<article class="cert-card"><span class="cert-category">${esc(x.category||'Credential')}</span><h3>${esc(x.name)}</h3><div class="issuer">${esc(x.issuer)}</div><div class="cert-meta"><div><span>Issued</span><strong>${esc(x.issued||'—')}</strong></div><div><span>Credential</span><strong>${esc(x.credentialId||'Verified')}</strong></div></div></article>`).join('')}
function renderPrinciples(p){$('#principlesGrid').innerHTML=(p.principles||[{title:'Reliability before novelty',text:'Choose technology that improves service outcomes and operational clarity.'},{title:'Automate repeatable work',text:'Reduce manual risk with tested, observable and reversible workflows.'},{title:'Measure what matters',text:'Use telemetry, service signals and evidence to guide decisions.'},{title:'Lead with ownership',text:'Connect engineering choices to users, teams and business continuity.'}]).map(x=>`<article class="principle"><b>${esc(x.title)}</b><p>${esc(x.text||x.details||'')}</p></article>`).join('')}
function displayUrl(v){return String(v||'').replace(/^https?:\/\//,'').replace(/\/$/,'')}
function linkHref(key,value){if(key==='email')return `mailto:${value}`;if(key==='phone')return `tel:${String(value).replace(/[^+\d]/g,'')}`;return value||'#'}
function setLinks(p){$$('[data-link]').forEach(a=>{const k=a.dataset.link,v=p[k]||'';a.href=linkHref(k,v);if(!v)a.setAttribute('aria-disabled','true')});$$('[data-contact-value]').forEach(el=>{const k=el.dataset.contactValue,v=p[k]||'';el.textContent=displayUrl(v)})}
function initRoles(p){let i=0;const el=$('#roleText');setInterval(()=>{i=(i+1)%(p.roles||[]).length;el.animate([{opacity:0,transform:'translateY(5px)'},{opacity:1,transform:'none'}],{duration:420});el.textContent=p.roles[i]},3200)}
function initClock(){const t=$('#consoleClock');const go=()=>t.textContent=new Date().toLocaleTimeString([],{hour:'2-digit',minute:'2-digit',second:'2-digit'});go();setInterval(go,1000)}
function initReveal(){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1});$$('.reveal').forEach(x=>io.observe(x))}
function initTilt(){if(matchMedia('(pointer:fine)').matches&&!matchMedia('(prefers-reduced-motion: reduce)').matches){$$('.tilt-card').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1200px) rotateX(${-y*4}deg) rotateY(${x*5}deg) translateY(-2px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}}
function schema(p){$('#schema').textContent=JSON.stringify({'@context':'https://schema.org','@type':'Person',name:p.name,jobTitle:p.headline,email:p.email,telephone:p.phone,url:p.website,address:{'@type':'PostalAddress',addressLocality:p.location},sameAs:[p.linkedin,p.github].filter(Boolean),alumniOf:(p.education||[]).map(x=>({'@type':'EducationalOrganization',name:x.institution})),knowsLanguage:(p.languages||[]).map(x=>typeof x==='string'?x:x.name)})}
async function init(){initTheme();initMenu();profile=await getProfile();renderFields(profile);renderOrbit(profile);renderStats(profile);renderPlatforms(profile);renderRibbon(profile);renderTools(profile);renderProjects(profile);renderExperience(profile);renderCerts(profile);renderPrinciples(profile);setLinks(profile);schema(profile);$('#languageSummary').textContent=(profile.languages||[]).map(x=>typeof x==='string'?x:x.name).join(' · ');initRoles(profile);initClock();initReveal();initTilt();$('#year').textContent=new Date().getFullYear()}
init().catch(err=>{console.error(err);document.body.classList.add('load-error')});